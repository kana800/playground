#include <stdio.h>



int main()
{
  int n;
  int arr[n+1];
  int size = sizeof(arr)/sizeof(arr[0]);
  int p = 2, sum = 0;
  
  for (int i = 0; i < size;) 

  while (p * p <= n)
  {
    if 
  }
  return 0;
}
